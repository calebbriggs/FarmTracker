<%= form_for(@herd) do |f| %>
  <% if @herd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@herd.errors.count, "error") %> prohibited this herd from being saved:</h2>

      <ul>
      <% @herd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :farm %><br />
    <%=  f.select(:farm_id,options_from_collection_for_select(Farm.all, :id, :name)) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
